# Handoff — LicenseSaathi: 00–15 DONE + merged; core build complete, only Mixpanel UI reports + prod-flag flips remain

**Written:** 2026-09-06 (updated through tickets 14 + 15 merge — PRs #28, #29). **Done so far:** ALL 16 tickets 00–15 — design foundation, scaffold, Supabase schema, rules engine, verified seed data, landing, questionnaire, result list, admin CRUD, field-notes display, auth + save, returning dashboard, checklist detail mark-done + printable pack, Mixpanel analytics, **reminder email (14)**, and **field-note capture (15)** — all finalized, reviewed, and merged to `main`, **plus the Option-A results→DB wiring (PR #18)**. The Must-ship spine, the full Should-ship companion, AND both optional cut-first tickets are complete AND instrumented: public flow (landing → category → questionnaire → verified route with citations + portal links) + save-a-checklist (email OTP) → returning dashboard with progress → checklist detail with per-licence mark-done + printable "bring these documents" pack + a "Share what happened" community field-note capture, a reminder-email retention loop (Vercel Cron + unsubscribe), and a Mixpanel event spine across the funnel. **Nothing is left to build.** Next session: finish the Mixpanel report setup (see "Mixpanel status" below) and/or flip the prod feature flags and ship.

## ⏰ Timing — read first
- **Submission target: 6 September 2026 (today).** The Solution PRD's honest ~4-day cut is spent. The **Must-ship spine is complete and merged** (03→04→05→06→07 + 09), and the **Option-A results→DB wiring is merged** (admin edits + field-notes can now reach users behind `FEATURE_RESULTS_FROM_DB`). Keep prioritizing Must-ship polish over new scope.
- **Must-ship** (demo is dead without it): Landing → Questionnaire → **verified ordered licence list with citations + portal links** (screens 1–3) + minimal admin CRUD to enter seed rules (screen 9). **03→04→05→06→07 + 09 are DONE + merged.** ✅ spine complete.
- **Should-ship** (companion thesis / North Star): Login + returning dashboard + checklist mark-done (screens 4–6 → tickets 10, 11, 12).
- **Cut-first, in order** if time runs out: field-note capture (15) → reminder email (14) → then trim checklist. If cut, still *pitch* the loop in the PRD (it's the vision), just don't build it.

## Next session's job
Core build is complete (00–15) — **nothing left to build**. Choose: (a) **finish the Mixpanel report setup** (see "Mixpanel status" below — token is done, only the UI reports remain), and/or (b) **flip the prod feature flags and ship**. Feature-flag reminder: to make the whole save/dashboard companion **live in prod**, set `FEATURE_SAVE_CHECKLIST=true` (and, for DB-backed results + field-notes, `FEATURE_RESULTS_FROM_DB=true` + `FEATURE_FIELD_NOTES=true`) on the target Vercel environment and redeploy — leave OFF until the DB is intentionally the source of truth for that environment. **For the reminder email + field-note capture (14 + 15), set `FEATURE_REMINDERS=true`** AND apply migration `0003_reminders.sql` by hand AND set the reminder env vars (`RESEND_API_KEY`, `CRON_SECRET`, `NEXT_PUBLIC_SITE_URL`) — see the "Reminders" box below.

### 📊 Mixpanel status (ticket 13) — DONE in code; setup PARTLY done
- **Done:** `NEXT_PUBLIC_MIXPANEL_TOKEN` is set in **local `.env.local`** AND **Vercel** (Kunal, this session). Verified live: events reach Mixpanel — a `POST https://api-js.mixpanel.com/track/` returned **200** with `list_viewed`/`flow_started` firing. Analytics is token-gated + no-op-safe; with no token it dev-console-logs `[analytics] <event>`.
- **REMAINING (Mixpanel UI, ~5 min — do in a browser, no code):** (1) build a **funnel** `flow_started → questionnaire_completed → list_viewed → checklist_saved → license_marked_done`; (2) build the **North Star** cohort/report = users who did `checklist_saved` **AND** `license_marked_done` (identify() ties the anonymous funnel to the signed-in user, so it's one journey).
- **Data-residency caveat:** the SDK sends to Mixpanel **US** servers by default. If the Mixpanel project is **EU/India residency**, events won't arrive until one line is added to `initAnalytics()` in `src/lib/analytics.ts` (`api_host`). US project = nothing to do.
- **Events + call sites:** `flow_started` (CategoryPicker), `questionnaire_completed` (QuestionnaireFlow), `list_viewed` (results), `portal_link_clicked` (StationCard→PortalLink), `checklist_saved` (SaveChecklist), `license_marked_done` (ChecklistItemToggle), `field_note_submitted` (**FieldNoteForm — wired in ticket 15**). All 7 events now have live call sites.

### 📮 Reminders setup (tickets 14 + 15) — DONE in code; setup NOT done
- **Ticket 14 (reminder email)** and **ticket 15 (field-note capture)** are merged (PRs #28, #29). Both are gated behind a single flag, **`FEATURE_REMINDERS`** (default OFF), and are a no-op without their secrets — safe in prod while dark.
- **REMAINING to make them live (no code):** (1) apply **`supabase/migrations/0003_reminders.sql`** by hand in the Supabase SQL editor (adds `users.reminders_opt_out`, `users.unsubscribe_token`, `saved_checklists.last_reminded_at` — same hand-apply flow as 0002; **not yet applied to the dev DB**); (2) on Vercel set `RESEND_API_KEY`, `CRON_SECRET`, `NEXT_PUBLIC_SITE_URL` (canonical origin for email links), optional `REMINDER_FROM`; (3) set `FEATURE_REMINDERS=true` and redeploy.
- **How 14 works:** `vercel.json` runs a daily cron (04:00 UTC) → `GET /api/reminders` (flag + `CRON_SECRET` bearer gated, service-role) → emails users with a saved checklist that is ≥3 days old, has pending items, isn't opted-out, and wasn't nudged in the last 7 days → stamps `last_reminded_at`. Email links back to `/checklist/[id]`; footer links to `/unsubscribe/[token]`. Send is a raw `fetch` to the Resend API (no new npm dep), no-op without `RESEND_API_KEY`.
- **How 15 works:** amber "Share what happened" link on each checklist stop → `/notes/[licenseId]` structured form (what happened / extra doc / extra fee) → inserts a `field_notes` row with `status='new'` via the anon client (existing RLS). New notes only appear in the ticket-08 display tier **after admin promotion to `status='promoted'`** (promotion is out of scope / schema-ready). Live-proven: a real `status='new'` row was inserted end to end on the dev DB.
- **Deliverability caveat (same as auth):** sandbox Resend (no verified domain) delivers **only to the account owner's own email**. Verify a sending domain (or Brevo) to reach real users.

### ⚠️ Auth setup gotchas (ticket 10 — learned the hard way, save the next person the time)
- **Email OTP, not phone** (owner decision — no SMS provider). `users.email` is the identity column; `users.phone` is now nullable (migration `0002_auth_email.sql`).
- **The free Supabase plan cannot edit email templates without custom SMTP.** You must connect an SMTP sender first (we used **Resend**: host `smtp.resend.com`, port `587`, username `resend`, password = a `re_...` API key, sender `onboarding@resend.dev`). Resend in sandbox (no verified domain) delivers ONLY to the account owner's own email — verify a domain (or use Brevo) to email anyone.
- **You must edit TWO email templates, not one.** A NEW user's first sign-in uses the **"Confirm signup"** template; a returning user uses **"Magic Link"**. Add `<p>Your code is: <strong>{{ .Token }}</strong></p>` to BOTH, or the code won't appear.
- The migration `0002_auth_email.sql` must be applied by hand in the Supabase SQL Editor (no Postgres connection string / CLI in this repo — see `supabase/README.md`).
- **Verified live end to end:** email → code → save created the `users` row, one `saved_checklists`, and per-licence `pending` `checklist_items`, all via owner-only RLS as the signed-in user (never service-role).

## Cross-ticket contracts locked this session (DO NOT drift)
- **Answer-key contract (from ticket 03)** — the questionnaire (06) presents these; the engine consumes them:
  - `turnover_band`: `under_12L | 12L_to_20L | 20L_to_40L | over_40L` (boundaries on the FSSAI ₹12L and GST ₹20L/₹40L lines)
  - `seating_band` (eatery): `none | under_50 | 50_plus`
  - `area_band` (retail/salon): `small | large`
  - `premises_type`: `on_premise | cloud_kitchen` (eatery) · `rented | owned` (retail/salon)
  - `alcohol`: boolean (eatery)
- **Engine ↔ DB reconciliation (ticket 04):** DB `rules.sequence` → engine `License.order`; `dependsOn: []` for every row (ordering via `order`, not a dependency graph — the engine's topo-sort degenerates to an order sort). DB `rules.condition` jsonb == engine `conditions` (array value = set-membership). No schema change was needed.
- **Routing contracts:**
  - Landing CTA → `/questionnaire/[category]` (`eatery|retail|salon`; category in the URL path).
  - Questionnaire completion → `/results/[category]?<answers as query params>` (answers in the URL: survive refresh, shareable, no login). **Ticket 07 renders this route for real.**
- **Fees (ticket 04 policy):** `govt_fee_inr` integer where clean (GST 0, FSSAI Basic 100); `null` + range-in-`description` where it varies. No schema change.

## Data trust status (ticket 04) — spot-check queue for Kunal
Verified spine (official source fetched): **Shops & Establishment** (enagar.gujarat.gov.in), **GST** (gstcouncil.gov.in), **FSSAI Basic + State** (foscos.fssai.gov.in). **Flagged (out of the verified spine, honestly labelled):** Fire NOC, Eating House / Trade / Health licences, Professional Tax, and the Gujarat liquor-prohibition row. **Root cause of most flags:** `ahmedabadcity.gov.in` (AMC) failed TLS verification from the build agent's sandbox — likely fine from a normal browser. **`docs/verified-data-review.md`** is the spot-check sheet; top priority is confirming the AMC / Fire-NOC rows (and the 50-seat Fire-NOC threshold used in 06 is one of these flagged values). Gujarat prohibition handled honestly: `alcohol:true` → a flagged row stating the real local truth (no standard venue liquor licence outside GIFT City), not a fabricated generic-India step.

## Repo & environment
- **Working dir / git repo:** `C:\Users\Asus\Desktop\builders-bible-excercises\Class Excercises\Ease of doing business solution`
- **Remote:** `https://github.com/kunalagarwal0210/License-Saathi.git`
- **TRUNK: `main`.** Branch from `origin/main`, merge back to `main` **via PR after cold review** — **never push to `main` directly**. This matches `docs/BUILD_WORKFLOW.md` §3, `docs/git-worktrees.md`, and `docs/feature-flags-and-staging.md`.
- **`gh` is authenticated** as `kunalagarwal0210` (repo scope) — you can open PRs. Merge to `main` only via PR; let Kunal approve merges (he authorizes on request).
- **Git identity:** Kunal / kunalagarwal0210@gmail.com. Commit trailers: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` + `Claude-Session: <session URL>`.
- **Worktree-per-ticket flow (works well):** `git worktree add -b <ticket-branch> .claude/worktrees/<ticket-branch> origin/main` (or the EnterWorktree tool) → `npm install` first → build → PR into `main`. Base off **origin/main**. Clean up worktree+branch (local+remote) after merge. Before every push: `git diff main..HEAD --stat` — only this feature's files should appear.
  - **Windows note:** `ExitWorktree --remove` often can't delete the worktree directory (file lock); follow with `git worktree prune` + `rm -rf .claude/worktrees/<name>` + `git branch -D <branch>` from the main checkout.
- Local-only, gitignored: `.claude/`, `.scratch/`, `.impeccable/build|mocks|review/`, `*.tsbuildinfo`.

## Source of truth (read first — do NOT duplicate)
- **`PRODUCT.md`** (project ROOT) — durable product truth (audience, verified-spine vs field-notes trust model, voice, scope).
- **`DESIGN.md`** (project ROOT) — the committed visual world "The Route". **Both live at ROOT, not `docs/`.**
- **`docs/UI_IMPLEMENTATION_SPEC.md`** — the finalized UI/frontend spec for tickets 05/06/07/09. **`docs/FIGMA_DESIGN_BRIEF.md`** — the token + per-screen-content contract.
- **`docs/verified-data-review.md`** — ticket-04 spot-check sheet (verified vs flagged rows + sources).
- `docs/BUILD_WORKFLOW.md`, `docs/git-worktrees.md`, `docs/ai-workflow-orchestration.md`, `docs/feature-flags-and-staging.md` — build/workflow references.
- `docs/Solution_PRD_LicenseSaathi.md`, `docs/Problem_Space_PRD_Micro-Licensing.docx`, `docs/Spec_LicenseSaathi.md` — PRDs + spec.
- `docs/tickets/00..15-*.md` — 16 tickets in dependency order.
- `README.md` — overview + local-dev instructions.

## Product in one line
Micro-licensing guidance for first-time small-business owners in **Ahmedabad only** (café/eatery, retail/kirana, salon). Short questionnaire → **ordered** set of licences, each with plain description, docs, govt fee, timeline, and a deep link to the official portal — human-verified, source-cited, date-stamped. Companion: save-as-checklist (phone-OTP), returning dashboard, printable pack, reminders. Data is two tiers **never mixed**: **verified spine** vs. labelled **community field-notes**. Stack: Next.js (Vercel) · Supabase (Postgres + phone-OTP) · Mixpanel.

## Status — what's DONE
- **Ticket 00 (design foundation) — DONE, approved, merged (PR #1).** `PRODUCT.md` + `DESIGN.md` at root. Name **LicenseSaathi**; voice friendly/encouraging; English MVP, built to add Gujarati without rework.
- **Ticket 01 (scaffold) — DONE, merged (PR #2).** Next.js 16 (App Router, TS) + Tailwind v4. DESIGN.md tokens in `src/app/globals.css` via `@theme`. Overpass + Hind fonts. Feature flags (`src/lib/flags.ts`). Vitest harness.
- **Ticket 02 (Supabase schema) — DONE, merged (PR #4); live dev DB proven.** `supabase/migrations/0001_initial_schema.sql` — 6 tables, 4 enums, RLS deny-by-default, two-tier invariant enforced structurally (`chk_verified_has_source`; `field_notes` separate table + `field_notes_public` view). Typed `src/lib/supabase/` (use `type`, NOT `interface`). Deferred: ticket 08 reads field-notes via `field_notes_public`.
- **Ticket 03 (rules engine) — DONE, merged (PR #9, merge `68e6cae`).** `src/lib/engine/` — pure `resolveLicenses(category, answers, rulesSource) → OrderedLicense[]`: condition matching (equality / set-membership / wildcard) + Kahn's topological sort with stable `(order, id)` tie-break; throws on cycles; no DB/UI imports. 13 TDD tests against in-memory fixtures. Defined the **answer-key contract** (above).
- **Ticket 04 (verified seed data) — DONE, merged (PR #10, merge `f20c4ab`).** `src/lib/data/verified.ts` — real Ahmedabad data (`VerifiedLicense` rows, one per category, + `verifiedRulesSource`). `scripts/seed-verified.ts` (idempotent Supabase seed; run with `npm run seed:verified` + dev env — not yet executed against live DB). `docs/verified-data-review.md` spot-check sheet. Persona tests pass the engine against real data. Verified vs flagged split + AMC TLS blocker (see Data trust status). `src/lib/supabase/types.ts` widened `RuleCondition` to allow array values (type-only).
- **Ticket 05 (landing + category picker) — DONE, Kunal-approved on running screen, merged (PR #11, merge `77035dc`).** `src/app/page.tsx` (real landing per spec §9) + `src/components/CategoryPicker.tsx` / `CategoryCard.tsx` / `CategoryIcon.tsx` + `src/lib/categories.ts` (categories + `isCategory()` + `getQuestionnaireHref()`). Selected-state token pattern established (route border + `route-tint` bg + filled check). Stub route `/questionnaire/[category]` (06 replaced it).
- **Ticket 06 (branching questionnaire) — DONE, Kunal-approved on running screen, merged (PR #12, merge `6a390a8`).** `src/lib/questionnaire.ts` (pure per-category question config + `showIf` branching + answer↔URL serialization) + `src/lib/questionnaire.test.ts` + `src/components/QuestionnaireFlow.tsx` + real `src/app/questionnaire/[category]/page.tsx`. One-question-at-a-time guided journey, dynamic progress, Back/Continue. Eatery branches (seating only if on_premise). On completion runs the engine on real data and routes to `/results/[category]?<answers>`. **Minimal results stub** at `src/app/results/[category]/page.tsx` (07 replaced it with station cards).
- **Ticket 07 (result list / Route screen) — DONE, Kunal-approved on running screen, merged (PR #14).** `src/lib/data/routeView.ts` (+ `.test.ts`, 11 tests) — pure engine-result→`VerifiedLicense` detail mapping + `formatFee`/`formatVerifiedDate`/`summarizeRoute`; `verifiedLicensesById` lookup added to `verified.ts`. `src/components/StationCard.tsx` (server component; native `<details>` for expand/collapse, no client JS). Real `src/app/results/[category]/page.tsx`: vertical **route rail** of numbered stops + station cards (description, document checklist, fee/timeline, portal deep link, verified seal `Verified · <date> · Source`). **Verified vs flagged** is the only status split, backed by data `status`: flagged spine rows get an honest red **"Confirm locally"** treatment (no green seal, no ✓). **Design call (approved):** the "X of N cleared" counter is intentionally omitted — "cleared" is per-user checklist state (10–12, needs login); header shows "N stops · V verified · F to confirm" instead. `formatVerifiedDate` uses a fixed month table (deterministic across Node/ICU — current ICU renders "Sept", not "Sep").
- **Ticket 08 (community field-notes tier + share) — DONE, merged (PR #16, merge `737aff1`).** `src/lib/data/fieldNotes.ts` (+ 6 tests) reads **only** the `field_notes_public` view, only `status='promoted'` rows; pure `groupFieldNotesByLicenseId` unit-tested; `getPublicFieldNotes` **never throws** (missing env/unseeded DB/network → empty Map). `src/components/FieldNotes.tsx` — amber sibling container **below** each station card (never inside), honours the two-tier invariant (never green/checkmark/"verified", renders nothing when empty). `src/components/ShareRoute.tsx` — Web Share API + clipboard fallback, accessible, fails silently. Results page integration fully gated behind `FEATURE_FIELD_NOTES` (flag off ⇒ no extra fetch, page unchanged). Read path proven against live dev DB with a seeded `promoted` note. **⚠️ Known limitation:** the tier does NOT render on `/results` yet — `/results` reads slug licence IDs from the TS module but `field_notes.license_id` is uuid, so the `.in()` query fails the uuid cast. Resolved by the Option-A wiring (in progress). Keep the flag OFF in prod until then.
- **Option-A results→DB wiring — DONE, Kunal-approved on running screen, merged (PR #18, merge `ff4605f`).** The results screen now reads `licenses`+`rules` from Supabase (real uuid ids end to end) instead of the TS module, **behind `FEATURE_RESULTS_FROM_DB` (default OFF, TS-module fallback on any DB error)**. `src/lib/data/dbSource.ts` — `server-only`; pure `mapDbRoute(licenseRows, ruleRows)` → engine `RulesSource` + uuid-keyed `licensesById` (order from `rules.sequence`), `loadDbRoute()` fetches via the **anon** client (public SELECT RLS on both tables) + 7 unit tests. `scripts/seed-verified.ts` fixed to run under `tsx` (script-local admin client, no `server-only`; minimal `.env.local` parser, no new dep) — `admin.ts` unchanged. **This fixes the field-notes slug/uuid bug** (08's tier now renders, proven on the running eatery route). Seed run against dev DB: 18 licences + 18 rules. **To go live:** set the flag(s) on Vercel + redeploy.
- **Ticket 10 (auth + save checklist) — DONE, verified live end to end, merged (PR #20, merge `dd3cd9c`).** Value-first **email OTP** (owner decision, not phone — no SMS provider) via Supabase Auth + `@supabase/ssr`; discovery stays login-free; auth is asked ONLY when the user clicks "Save my checklist" on results. `supabase/migrations/0002_auth_email.sql` (relax `users.phone`, add `users.email`, owner-insert RLS policy) — applied to dev DB. New: `src/lib/supabase/server.ts` (session-aware server client), `browser-auth.ts` (cookie session client), `src/middleware.ts` (session refresh only, no route guards), `src/app/results/[category]/actions.ts` (`saveChecklist` — validates via `getUser()`, writes `users`/`saved_checklists`/`checklist_items` as the user through RLS, never service-role), `src/lib/checklist/saveChecklist.ts` (+ tests), `src/components/SaveChecklist.tsx` (email→code→saved UI, accessible, retry on error). Gated behind `FEATURE_SAVE_CHECKLIST`. **Setup gotchas: see the "Auth setup gotchas" box under Next session's job (custom SMTP required to edit templates; edit BOTH Confirm-signup AND Magic-Link templates).** Non-blocking: Next 16 deprecates `middleware.ts` → `proxy.ts` (still works) — follow-up.
- **Ticket 11 (returning dashboard) — DONE, verified live end to end, merged (PR #22, merge `d51aadd`).** `src/app/dashboard/page.tsx` (server, `force-dynamic`, gated `FEATURE_SAVE_CHECKLIST`): signed-in user sees one card per saved checklist — category, saved date, "X of N done" + progress bar, pending-names preview (first 3 + "+N more"), Resume link; empty state + error state; not-signed-in shows `src/components/DashboardSignIn.tsx` (email-OTP, no save, `router.refresh()` on success). `src/lib/checklist/dashboard.ts` `summarizeChecklistItems` (+ 5 tests). Nested RLS-scoped query `saved_checklists → checklist_items → licenses(name)`. Resume rebuilds the results URL from saved answers via `getResultsHref` (ticket 12 repoints Resume to the real `/checklist/[id]` detail). `SaveChecklist` success state got a "View my saved routes" link to `/dashboard`.
- **Ticket 12 (checklist detail — mark-done + printable pack) — DONE, verified live end to end, merged (PR #24, merge `4881777`).** `src/app/checklist/[id]/page.tsx` (server, `force-dynamic`, gated `FEATURE_SAVE_CHECKLIST`): RLS-scoped fetch (foreign/missing id → `notFound()`), reconstructs the ordered route (`loadDbRoute`→`resolveLicenses`→`buildRouteStations`), overlays per-licence done/pending from `checklist_items`, renders the numbered route rail (reusing `StationCard`) + a `ChecklistItemToggle` per stop + done/total progress + the document pack. `src/app/checklist/[id]/actions.ts` `setChecklistItemStatus` (owner-RLS **upsert**, never service-role, `revalidatePath`, never throws). `src/components/ChecklistItemToggle.tsx` (optimistic, revert+error, `router.refresh()`). `src/components/PrintButton.tsx` (`window.print()`). `src/lib/checklist/documentPack.ts` `buildDocumentPack` (+ 7 tests) — case-insensitive dedupe across licences. `src/app/globals.css` got one `@media print` block (`.print-hide` on controls/rail → prints title + doc pack). Dashboard Resume now → `/checklist/[id]`. State DB-backed, persists. (One test item left marked done on the dev checklist — harmless.)
- **Ticket 13 (Mixpanel analytics) — DONE, verified live (events reach Mixpanel), merged (PR #26, merge `6672a39`).** Token-gated, no-op-safe wrapper `src/lib/analytics.ts` (`ANALYTICS_EVENTS`, `initAnalytics`, `track`, `identifyUser`; SSR-guarded, idempotent, try/catch throughout, dev console fallback) + `analytics.test.ts`. Client components `AnalyticsInit` (in layout), `TrackOnMount` (`list_viewed`), `PortalLink` (`portal_link_clicked`), `AnalyticsIdentify` (authed pages). 7-event spine wired to its surfaces; `identifyUser(user.id)` on save + authed pages makes the North Star computable. Only new deps: `mixpanel-browser` (+ dev `@types/mixpanel-browser`). **See "Mixpanel status" above for what remains (UI reports only).**
- **Ticket 14 (reminder email) — DONE, verified (build + live smoke), merged (PR #28, merge `3f6308f`).** Cut-first retention loop, all behind `FEATURE_REMINDERS` (no-op without secrets). `supabase/migrations/0003_reminders.sql` (`users.reminders_opt_out`, `users.unsubscribe_token` + unique index, `saved_checklists.last_reminded_at` — **hand-apply, NOT yet applied to dev DB**). Pure + tested: `src/lib/reminders/selectReminders.ts` (11 tests — pending>0, has email, not opted-out, ≥3d old, not nudged in 7d) + `reminderEmail.ts` (6 tests — subject/html/text, self-declared progress only, unsubscribe footer). `sendEmail.ts` (raw `fetch` to Resend, never throws, no-op without `RESEND_API_KEY`, no new npm dep). `src/app/api/reminders/route.ts` (`GET`, flag + `CRON_SECRET` bearer gated, service-role candidate query via a `!inner` users join, sends + stamps `last_reminded_at`, one failed send never aborts the batch). `src/app/unsubscribe/[token]/page.tsx` (neutral, idempotent, token IS the authz, never leaks whether a token exists). `vercel.json` daily cron 04:00 UTC. `src/lib/supabase/types.ts` extended. Live smoke: 401 without the secret; the join query reaches Postgres (fails only on the un-applied `0003` column, proving the SQL shape); unsubscribe page renders on-brand. **See "Reminders setup" above to go live.**
- **Ticket 15 (field-note capture) — DONE, verified live end to end, merged (PR #29, merge `e2b9dc9`).** The community-tier capture form, behind `FEATURE_REMINDERS`. **No migration needed** (the `field_notes` table + `field_notes_insert_public` RLS already support it). `src/lib/fieldNotes/captureForm.ts` (pure, 22 tests — required/trim/max-length `whatHappened`, empty→null + 300-char clamp `extraDoc`, non-negative-integer `extraFee`). `src/app/notes/[licenseId]/page.tsx` (server, flag-gated, fetches licence name via anon client for display + existence check, 404 on flag-off/unknown id). `src/app/notes/[licenseId]/actions.ts` `submitFieldNote` (inserts `field_notes` via the **anon** client — never service-role — never sets `status` so the DB default `'new'` + RLS check both hold, never throws). `src/components/FieldNoteForm.tsx` (amber "Field notes, not law" framing = two-tier invariant; accessible state machine like `SaveChecklist`; fires `field_note_submitted` — **first call site of that event**). Checklist detail got a quiet amber "Share what happened" link per stop → `/notes/[id]`. New notes are `status='new'` → only surface in the 08 display tier after admin promotion (out of scope, schema-ready). Live-proven: form renders the licence name, a real `status='new'` row was inserted, success state shown.
- **Ticket 09 (admin CRUD) — DONE, Kunal-approved on running screen, merged (PR #15).** Protected `/admin` panel behind the `FEATURE_ADMIN` flag (**flag-only gate, no password** — chosen for this ticket); `src/app/admin/layout.tsx` is the single gate (`notFound()` when off) + `dynamic = "force-dynamic"`. Licences + rules **list/add/edit/delete**; verify-stamp enforced (`status='verified'` needs `source_url` + `last_verified_date`, mirroring `chk_verified_has_source`). Writes/reads via the **server-only service-role client** (`src/lib/supabase/admin.ts`) through server actions + server components. Pure parse/validate extracted (`src/lib/admin/licenseForm.ts`, `ruleForm.ts`) + **22 unit tests**. **Decision (Option A):** admin writes to the DB; the public results screen + engine still read the TS module (`src/lib/data/verified.ts`) — results→DB wiring is a follow-up. Write path verified end to end against the live dev DB. Out of scope (schema-ready): flag-review, field-note promotion.

## Method used this session (orchestrator + workers)
- **⚠️ Builds MUST be subagent-driven (Kunal's explicit instruction, this session).** The orchestrator does NOT write ticket implementation in the main session — it plans, dispatches a fresh worker subagent to build in the worktree, then independently reviews/verifies and runs the design gate. (Ticket 07 this session was built in the main session *before* this correction and shipped as-is; ticket 09 was correctly subagent-built.)
- **Explanations to Kunal in Simplified Technical English (the `simpexp` skill).** Kunal asked for STE for the whole session — short active sentences, one idea each, define terms. Applies to explanations/decision write-ups, not code or commit messages.
- **Pro-subscription-friendly:** worker subagents run on **Sonnet** (not Opus) to fit Pro rate limits; the orchestrator (Opus 4.8) does the cold review itself rather than spawning an Opus-max reviewer. One fresh Sonnet worker subagent per ticket, TDD/build-first, `npm install` in the fresh worktree first.
- **Verification before merge:** orchestrator independently re-runs `test`/`typecheck`/`lint`/`build`, checks `git diff --stat origin/main` scope, and reads the key files before opening the PR.
- **UI design gate = build-then-review on the running app:** worker builds from tokens + spec; orchestrator runs the dev server (`npm run dev -- -p <port>`), screenshots via the Chrome tools, and sends previews to Kunal with `SendUserFile`; Kunal approves the running screen, then merge. (No separate `/impeccable shape` mock gate for the deadline.)
- **Testing corner (all UI tickets):** no React Testing Library in the repo, so interaction/render is not unit-tested; instead the pure logic (categories, questionnaire, engine, routeView, admin forms) is extracted into modules and unit-tested. Adding RTL is an open follow-up if wanted.
- **Worktree env:** a fresh worktree has no `.env.local` (gitignored, not copied). Copy it from the main checkout into the worktree when the ticket needs live Supabase (e.g. admin/09), and set `FEATURE_ADMIN=true` there for local review. Never commit `.env.local`.

## Open follow-ups (small, unblock later work)
- **Wire results → DB (Option A) — DONE (PR #18).** Results reads `licenses`+`rules` from Supabase (uuid ids) behind `FEATURE_RESULTS_FROM_DB`, TS-module fallback on DB error. Seed script fixed + run (18 licences, 18 rules). Field-notes slug/uuid bug fixed. **Remaining to make it live:** flip `FEATURE_RESULTS_FROM_DB` (+ `FEATURE_FIELD_NOTES`) on the target Vercel environment and redeploy. Minor edge (acceptable): an *empty/unseeded* DB yields an empty route — only DB *errors* trigger the TS fallback, so only turn the flag on where the DB is seeded.
- **Test rows in the dev DB.** "Test Café Signage Permit" (eatery, flagged, uuid `140dc58d-…`, admin-panel write-path proof) + a promoted field-note on it (uuid `6abc237f-…`). Option A then seeded the real 18 verified licences + rules, and added one **promoted field-note** (uuid `d7a20a01-…`, "notarized rent agreement") on the real eatery **Shops & Establishment** licence (uuid `2e2d7fd5-…`) — this is the one that renders on the eatery route when the flags are on. All safe to delete; keep the `2e2d7fd5` note if you want the demo tier visible. Admin delete uses native `window.confirm`. **Ticket-15 smoke also left one `[TEST NOTE — ticket 15 smoke]` field-note (`status='new'`, on licence `2e2d7fd5`) — invisible publicly, safe to delete.**
- **Ticket-04 flagged copy.** Flagged rows' `description` carries developer research prose ("FLAGGED: …") that shows on the results card. Recommend a short ticket-04 copy pass.
- **Admin delete UX.** `window.confirm` is functional but crude and hard to test — later polish to an inline confirm.
- **`.gitignore` the Next-16 agent files.** Running `next dev`/`next build` auto-generates `AGENTS.md` + `CLAUDE.md` at the repo root (Next's `generate-agent-files.js`). They were deliberately kept OUT of the 14/15 PRs; add both to `.gitignore` so future worktrees don't keep flagging them as untracked.
- **Reminder deliverability + prod DB.** To actually send reminders to real users you need a **verified Resend domain** (sandbox reaches only the owner's email) and migration `0003` applied to whichever DB the environment points at (still dev-only today; a separate **production** Supabase project is the pre-launch TODO).

## The design system in brief ("The Route")
Deliberately OFF the government-paper cliché. **Sequenced civic wayfinding:** licensing as a numbered, signposted journey to "shop open, legal."
- **Palette:** light-first, one-job-per-color — indigo `route` (path), green `verified` (spine/✓/date), amber `note` (field-notes), red `flag` (re-verify/error). Dark = role swaps.
- **Type:** Overpass (signage) + Hind (body, language-expansion path).
- **Signature primitives:** route rail + stop nodes, **station card**, **verified seal** (green ✓ "verified on <date>" + source), **traveller-note** (amber "field notes, not law").
- **Load-bearing invariant:** verified spine and field-notes are **never mixed** — different color, container, language.

## Build frontier — where we are
`01 → {00, 02} → {03, 05, 09} → 04 → 06 → 07 → {08, 10} → {11, 13} → 12 → 14 → 15`
- **Done + merged:** 00–15, **Option-A** (PR #18). Recent PRs: 10 (#20), 11 (#22), 12 (#24), 13 (#26), **14 (#28), 15 (#29)**.
- **In progress:** nothing — the whole ticket list is built.
- **Only remaining (no code):** the Mixpanel UI report setup (see "Mixpanel status") and flipping prod feature flags (`FEATURE_REMINDERS` + migration `0003` + Resend/cron env — see "Reminders setup").

## Infra status — Kunal's accounts
- **Supabase — LIVE for dev.** Dev project exists, migration applied, round-trip PASSES. Keys in local `.env.local` (gitignored). Still TODO: a **separate production** project before real launch. The **admin panel (09) reads/writes the dev DB** via the service-role client. The **public results screen can now read the DB** (Option A, PR #18) behind `FEATURE_RESULTS_FROM_DB` — default OFF, so it reads the TS module `src/lib/data/verified.ts` until the flag is flipped per environment. The ticket-04 bulk seed **has now run** against the dev DB (18 licences + 18 rules) via the fixed `scripts/seed-verified.ts`. **Auth (ticket 10) is LIVE on dev:** migration `0002` applied, custom SMTP connected (Resend), both email templates carry `{{ .Token }}`, and a real email-OTP sign-in + save was proven (one `users` + one `saved_checklists` + 6 `checklist_items` rows exist as test data — safe to delete). **Migration `0003_reminders.sql` (tickets 14/15) is NOT yet applied to the dev DB** — apply it by hand before turning `FEATURE_REMINDERS` on anywhere; ticket-15 field-note inserts already work against the current schema (no migration needed for 15).
- **Vercel — LIVE.** Repo connected; Production branch = `main`; 3 Supabase keys set on all environments (dev project). Deployed: **https://license-saathi.vercel.app/**. Every PR gets a preview URL. After changing any env var, **redeploy** (vars bake at build time).

## How to work (Kunal's stated preferences)
- Engineering-manager posture: clarify goal, scope, flag risk early, give a recommendation at decision points. Direct and concise; lead with outcome; name assumptions/cut corners.
- **Builds are subagent-driven** (see Method). The main session orchestrates + reviews; a worker subagent writes the ticket code.
- **Explain in Simplified Technical English** (`simpexp`) — short active sentences, define terms.
- **One surface at a time; each finalized and approved before the next.**
- Branch-based; worktree per ticket; PR into `main` after cold review; **never push to `main` directly**; let Kunal merge (or he authorizes).
- He follows from another device — use `SendUserFile` to put deliverables (screenshots/preview) in front of him.

## Suggested skills for the next agent (Skill tool)
- **`superpowers:brainstorming`** — before UI/creative work.
- **`superpowers:test-driven-development`** — Vitest harness set up.
- **`superpowers:writing-plans`** / **`superpowers:executing-plans`** — sequencing under time pressure.
- **`artifact-design`** — if showing designs as artifacts/mockups.

## Notes
- No secrets/PII committed. Env files gitignored; `.env.example` has names only.
- Toolchain: Node v24, npm 11. `impeccable` + `handoff` skills installed globally.
